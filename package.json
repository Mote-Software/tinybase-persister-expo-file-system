{
  "name": "tinybase-persister-expo-file-system-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm --filter @mote-software/tinybase-persister-expo-file-system build",
    "test": "pnpm --filter @mote-software/tinybase-persister-expo-file-system test",
    "dev": "pnpm --filter @mote-software/tinybase-persister-expo-file-system dev",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "release": "semantic-release"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^11.0.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^10.0.0",
    "@semantic-release/npm": "^12.0.0",
    "@semantic-release/release-notes-generator": "^12.0.0",
    "semantic-release": "^23.0.0"
  }
}
